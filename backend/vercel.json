{
  "version": 2,
  "builds": [
    { "src": "api/**/*.js", "use": "@vercel/node" }
  ],
  "installCommand": "cd backend && npm install",
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.js" }
  ]
}
